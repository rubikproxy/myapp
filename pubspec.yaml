name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  webview_flutter: ^3.0.4
  # simple_animations: ^5.0.2  # Add this line for supercharged

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/rubik.png
    - assets/image.png
    - assets/logo.png

